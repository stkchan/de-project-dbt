version: 2

models:
  - name: bronze_dim_date #  Must match the filename of a model -- including case sensitivity.
    config:
      materialized: view
      schema: bronze

  - name: bronze_dim_product
    config:
      materialized: view